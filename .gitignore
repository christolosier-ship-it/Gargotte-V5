# Local environment / secrets
.env
.env.local
.env*.local

# Neon local project context
# `neon link` writes local project/branch context here; keep it out of Git.
.neon
.neon/

# Node tooling
node_modules/

# OS files
.DS_Store
